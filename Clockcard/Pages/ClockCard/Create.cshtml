@page
@model Clockcard.Pages.ClockCard.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Clock</h4>
<hr />

@*<button id="clockbutton" onclick="ClockInJs()" >Clock In</button>*@

<hr />

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                @*alter the dropdown menu *@
                @Html.DropDownListFor(m => m.Clock.EMPREF,
                                         new SelectList(
                                             Model.EmployeesDict,
                                             "Key",
                                             "Value"), new { @class = "form-control" })
                @*Old way to alter the dropdown menu *@
                @*                <label asp-for="Clock.EMPREF" class="control-label"></label>
        <input asp-for="Clock.EMPREF" class="form-control" />
        <span asp-validation-for="Clock.EMPREF" class="text-danger"></span>*@
            </div>
            <div class="form-group">
                <label asp-for="Clock.STARTTIME" class="control-label"></label>
                <input asp-for="Clock.STARTTIME" class="form-control" />
                <span asp-validation-for="Clock.STARTTIME" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Clock.ENDTIME" class="control-label"></label>
                <input asp-for="Clock.ENDTIME" class="form-control" />
                <span asp-validation-for="Clock.ENDTIME" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        function ClockInJs() {
            
            //this function created in order to call CS methods
            @{
                 await Model.HandleClock();
            }
            console.log("Clicked");
        }
    </script>
}
